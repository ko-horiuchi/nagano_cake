<h1>ジャンル編集ページ</h1>
  <%= form_with model: @genres, url: admin_genre_path, local:true do |f| %>
    <label for="name">ジャンル名</label></br>
    <%= f.text_field :name %></br>
    <%= f.submit 'ジャンル名編集' %>
  <% end %>
  
  
  <p><%= link_to "ジャンル一覧に戻る", admin_genres_path %></p>